# DEPRECATED: Use ci-cd.yml instead
# This file is kept for backward compatibility
# Please migrate to the new workflow: .github/workflows/ci-cd.yml

name: CI/CD Pipeline with Allure (Legacy)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
    - name: Info
      run: |
        echo "This workflow is deprecated. Please use ci-cd.yml instead."
        echo "See DEPLOYMENT.md for details."