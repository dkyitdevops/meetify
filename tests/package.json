{
  "name": "meetify-tests",
  "version": "1.0.0",
  "description": "E2E tests for Meetify",
  "scripts": {
    "test": "mocha --timeout 60000 tests/*.test.js",
    "test:api": "mocha --timeout 10000 tests/api.test.js",
    "test:e2e": "mocha --timeout 60000 tests/e2e.test.js"
  },
  "dependencies": {
    "mocha": "^10.2.0",
    "puppeteer": "^21.0.0",
    "chai": "^4.3.7"
  }
}